# Pipecat Voice Service Dependencies

# ?? Pipecat AI (core + WebRTC + whisper) ??????????????????
pipecat-ai[silero,webrtc]>=0.0.60

# ?? Web Server ????????????????????????????????????????????
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ?? HTTP Client (for remote STT/TTS) ?????????????????????
aiohttp>=3.9.0

# ?? Utilities ?????????????????????????????????????????????
python-dotenv>=1.0.0
numpy>=1.24.0

# ?? Optional: local whisper (GPU only) ???????????????????
# Uncomment if running with STT_MODE=local
# pipecat-ai[whisper]>=0.0.60
# faster-whisper>=1.1.0

